# 更新记录

## v1.4.30

- 点击展开面板外部可收起设置、分析与附着下载面板；设置未保存的修改按取消处理。
- 不关闭独立下载器；面板内部点击、右键菜单及模态子对话框不触发外部收起，不吞掉原本的鼠标点击。

## v1.4.29

- 下载面板改为相对悬浮条居中对齐，与设置面板保持相同展开方向；去除原先靠右偏移和额外间隙。
- 包含 v1.4.25–v1.4.28 的原生分析接入、完整版下载器主题统一、后台位置扫描优化。

## v1.4.28

- 将聊天框 UI Automation 位置扫描移至后台，界面线程只读取缓存，避免跨进程扫描拖慢按钮和悬浮反馈。
- 同一时间仅运行一次扫描；缓存随窗口平移，窗口切换/尺寸改变校验后再使用，超过 5 秒的旧位置不再显示。
- 新增慢扫描、连续读取、拖动平移与跨窗口缓存隔离回归测试。

## v1.4.27

- 完整版 MSIX 下载器共用当前设置主题，使用不透明背景、统一状态卡片与日志配色，移除旧浅绿玻璃背景。
- 保留公众号 Logo、署名和全部下载/安装功能；主次按钮统一配色，保留悬浮与按下反馈。

## v1.4.26

- 接通 Codex 原生分析的产品用量、模型用量、模型活动、使用端、技能和插件六个视图，保留官方百分比 / credits / 轮次 / 调用次数单位。
- 只提供 7 天和 30 天，按所选期间查询官方接口，截止 UTC 昨天；不使用本地会话统计，不对缺失日期补零。
- 分析面板延续设置宽度和主题，增加每日图表、明细翻页、分模块错误状态；后台查询不阻塞按钮。
- 只读复用本机 Codex 登录态，仅访问固定官方 HTTPS 域名，禁用重定向，不保存或刷新凭据；兼容 HTTPS_PROXY 与 TLS 1.2。

## v1.4.25

- 分析面板改为直接读取 Codex 官方 account/usage/read 每日 Token，移除运行界面中的本地任务/工具/模型会话统计。
- 今日、7 天、30 天按官方日期桶汇总，显示已返回日期的覆盖范围；缺失日期不补零，累计 Token 单独标明全周期。
- 增加官方数据获取时间、最近返回日期和异常/空数据状态，刷新按钮可重新读取分析；过期异步响应不会覆盖新面板。
- 原生设置页的套餐/产品/模型/技能/插件分析明细尚未接通，明确显示不可用，不使用其他计数替代。

## v1.4.24

- 按新设计统一七套主题的面板配色：彩字浅白底、橙色暖白底、原生灰白底；设置与分析使用不透明表面、细边框及常规字重。
- 设置、分析和嵌入下载面板统一目标宽度 688；下载面板沿工具条右边缘锚定，右键菜单跟随当前主题。
- 新增蓝色 >_ 托盘图形，左键可打开完整版下载器；切换附着面板撤销未保存设置。
- 调整完整版下载器基础配色，保留现有下载与安装服务。
- 附界面设计交接样例。当前本地分析的去重和指标口径仍待修正，不等同于官方分析；完整版下载器多 Tab、共享下载任务和多状态托盘尚未完成。

## v1.4.23

- “分析”改为在悬浮用量条内展开，不再创建独立窗口；顶部任务栏向下展开，聊天框位置保持设置面板同样的向上锚定展开。
- 分析面板沿用当前主题的表面、边界、文字和按钮状态，并保留“今日 / 7 天 / 30 天 / 数据说明”页签。

## v1.4.22

- 重做“分析”页为与设置面板同宽的原生浅色布局，提供“今日 / 7 天 / 30 天 / 数据说明”页签，并跟随当前主题的表面、边界与强调色。
- 新增本机会话元数据汇总：任务、工具调用、活动图表和模型记录按时间范围展示；只读取时间、事件类型和模型字段，不读取、不保存、不上传对话正文。

## v1.4.21

- 设置齿轮右侧新增“分析”图标，打开 Codex 原生浅色风格的用量分析页。
- 分析页展示本机已验证的套餐、5 小时/周额度、重置券、累计 Token、任务状态与数据来源；未提供按模型、工具或小时统计的字段会明确留空，不显示推测值。

## v1.4.20

- 系统托盘右键的“打开 Codex 更新下载器”改为独立打开完整更新器；Codex 已退出时也不会被悬浮层跟随逻辑关闭。

## v1.4.19

- 修复系统托盘“打开下载器”在菜单收起的焦点切换期间被跟随计时器立即隐藏的问题。

## v1.4.18

- 系统托盘图标右键菜单新增“打开下载器”；齿轮右键菜单不再重复该入口。

## v1.4.17

- 齿轮右键菜单新增“打开下载器”，可直接展开 MSIX 更新面板；若面板已打开则保持显示，不会误触关闭。

## v1.4.16

- 更新面板的五个操作固定为等宽单行网格；Windows 缩放或大字体下也不会再把“更多选项”折到下一行。

## v1.4.15

- 系统托盘菜单的“退出工具”现在立即退出，不再弹出二次确认；更新菜单内的“退出程序”仍保留确认以防误触。

## v1.4.14

- 重做下载面板的原生浅色层级：信息区、操作区与日志区采用更紧凑的留白和中性边界，彩字主题不再把橙色描边铺满全部按钮。
- 右上角改为自绘无边框关闭图标，恢复可见的关闭操作与悬停、按下反馈。

## v1.4.13

- 用量读取优先连接当前 Codex 桌面版自带的 CLI，不再优先使用可能遗留的 sandbox 副本；app-server 通信固定使用 UTF-8，避免中文 Windows 环境下的 JSON 响应解析失效。
- 程序启动即后台刷新用量与雷达，缓存恢复时保留已验证字段标记；刷新成功会写入最新时间，减少旧额度长期停留在界面的情况。
- Codex 更新面板继续沿用设置面板的同一主题表面与文字颜色规则；刷新、设置、下载入口点击不再排入下一轮界面消息队列。

## v1.4.12

- Codex 更新窗口采用与设置面板一致的 640 × 446 逻辑尺寸，日志区随空间扩展；同时跟随当前外观主题同步背景、边框、强调色与按钮。

## v1.4.11

- 重做 Codex 更新面板的 DPI 自适应布局：标题、状态、操作与日志区改由布局容器管理，移除手动二次字体缩放，避免高缩放/大字体下的重叠和截断。

## v1.4.10

- 刷新、设置、下载按钮改为直接接收透明悬浮层的鼠标消息；悬浮立即显示原生浅灰底与描边，点击先呈现按下状态再执行操作。

## v1.4.9

- 下载面板改为复用 Codex 主窗口的实际 DPI 比例，确保在不同显示缩放下外框、按钮、字体同步缩放。

## v1.4.8

- 修复 MSIX 下载面板在部分 Windows 显示缩放下边框与字体比例失配的问题，面板现按显示器 DPI 整体缩放。

## v1.4.7

- 新增常驻系统托盘图标，右键菜单可直接选择“退出工具”；关闭重置提醒后图标仍保留，方便管理程序。

## v1.4.6

- 修复首次打开 Codex 更新面板时 Windows 使用默认坐标造成的滑入效果：窗口现会先计算锚点、后显示。

## v1.4.5

- 顶部用量条保持简短的概率文案；完整预计窗口放在顶部雷达悬浮窗中展示。运营方确认的预告也可正常触发该悬浮窗。

## v1.4.4

- 重置预告在有效期内显示置信度与完整预计窗口，例如“65%会重置 · 预计：9月1日22:00—9月2日21:59”。

## v1.4.3

- 重置预告在有效期内按置信度显示为“65%会重置”，避免被误解为“重置进行中”。

## v1.4.2

- 修复整日重置预告被误判为瞬时事件的问题：识别官方状态源的 `schedulePrecision: date`，在完整预告窗口内持续显示重置信号。

## v1.4.1

- 工具正式更名为“Codex 用量与更新助手”；程序标题、设置页、通知、安装器、桌面快捷方式与带版本号的分发文件名同步更新。
- 新安装包命名为 `blues19-CodexUsageUpdateAssistant-Setup-1.4.1.exe`；保留原有 AppId 和内部主程序名，覆盖升级时继续保留已有设置。
- 兼容 Codex Runway 新增的“运营方确认、无 X 原帖”的重置预告说明；仅当来源确认为 operator 时接受，未知说明和不匹配来源仍会拒绝。

## v1.4.0

- 在 Overlay 内置 Codex 桌面版 MSIX 更新器：从微软官方目录查询、解析 Windows Update 分发、下载后校验摘要并通过系统部署接口安装。
- 悬浮条新增下载入口，安装包默认保存到桌面；保留检查更新、仅下载、安装本地包和代理设置。
- 刷新、设置与下载按钮统一为适配深浅和彩字主题的自绘图标，并增加悬停与按下反馈。

## v1.3.51

- 兼容 Codex Runway 新增的高概率重置预告说明，避免整份雷达数据因单条新说明不在旧白名单中而被判定为“雷达离线”。
- 保留事件类型、来源与说明文本的精确校验；未登记的预告说明仍会被拒绝，不扩大信任边界。
- 新增新预告说明的正向回归测试与未知说明的反向测试，并通过当前线上雷达数据验证。

## v1.3.50

- “显示位置”改为直接选择的三个并列按钮：顶部任务栏、聊天对话框内、聊天对话框下面；当前项高亮，不再点击整行循环切换。
- 悬浮设置页与普通设置窗口保持一致的直接选择体验。
- 感谢猫de天空之城、乐缘、谢谢你提醒我，提供问题定位、修复建议与验证。

## v1.3.49

- 修复“无胶囊”模式在浅色标题栏上的低对比文字：用量、重置信号和彩字主题均改用适合浅色背景的深色主题墨色，不再发白发虚。
- “用量排版”直接展示“一行概览／两行详情”两个选项；“胶囊风格”直接展示“圆角／小圆角／无胶囊”三个选项，当前选项高亮，不再通过点击整行循环切换。
- 悬浮设置页和普通设置窗口使用相同的直接选择方式。

## v1.3.48

- 顶部任务栏不再按 Codex 窗口内部宽度强制压缩：会在当前屏幕的安全范围内保留完整字段，累计 Token 等长值不再自动显示为省略号。
- 顶部条在屏幕边缘会保持可见；聊天对话框内和聊天对话框下面仍严格遵守各自输入框的安全边界。

## v1.3.47

- 修复顶部任务栏收起时重复绘制外层胶囊的问题：左右两段空色块已移除，只保留实际的额度、重置信号和操作按钮。
- 荧光蓝、渐变橙、渐变粉与自定义主题的胶囊底色改为更实的对应色相；磨砂、轻盈白和彩字仍保留亮底表面，保证各主题的文字对比。
- 主题预览补充自定义主题，并使用真实额度字段生成顶部收起态截图，便于检查所有主题。

## v1.3.46

- 顶部任务栏字号默认调整为 12pt，升级自 v1.3.45 的原默认 8.5pt 会自动迁移；顶部可继续增大到 18pt。
- 顶部重置信号按完整文字实际宽度布局，不再显示省略号；扩大字号时会同步扩宽状态块。
- “暂无重置信号”取消左侧状态圆点，减少无实际告警含义的视觉噪声。

## v1.3.45

- 新增三个彼此独立的字号：顶部任务栏、聊天对话框内、聊天对话框下面。三个位置可分别按 0.5pt 调整并持久保存，切换显示位置不会覆盖其他位置的字号。
- 内联设置将三个字号压缩成一行“顶／内／下”微型步进控件；传统设置窗口也同步提供三项字号，方便从任一路径调整。
- 字号改变会同时更新可用宽度与文字绘制，避免更改后发生截断或保留旧宽度。

## v1.3.44

- 新安装默认显示在顶部任务栏；已保存的显示位置仍会保留，不会被升级重置。
- 顶部的重置信号、刷新和设置按钮统一使用当前胶囊风格的底色、边框和圆角；状态仅通过左侧小圆点区分，不再用整块状态色破坏视觉一致性。

## v1.3.43

- 悬浮层改为直接监听 Codex 窗口的位置变化：拖动窗口时主条、重置雷达预告和首次指引会按同一位移同步移动，不再等待 250ms 定时刷新而留下残影。
- 两行额度胶囊按实际文字宽度收紧，并使用真实字形的视觉中线绘制，改善中英文混排和 PRO 标签偏高的问题。

## v1.3.42

- 刷新与齿轮改为同尺寸、同圆角、同主题描边的成对操作按钮；两者共享垂直中线和固定间距。

## v1.3.41

- 修复顶部任务栏一行布局：额度、重置信号、刷新和齿轮改为连续的整体居中组，不再分散在栏位两端。
- 一行胶囊、重置信号和操作按钮统一按栏位垂直中心对齐；聊天对话框内则改为对齐“完全访问”所在工具栏中线。

## v1.3.40

- 修复“聊天对话框内”的彩字主题：用量文字现在使用独立的深色彩虹渐变，在白色输入框上仍保持清晰。
- 两行详情改为逐行独立胶囊；圆角、小圆角矩形和无胶囊三种风格都有明确区别，不再显示一整条大色块。

## v1.3.39

- 收紧设置面板最大宽度；在“聊天对话框内”展开时，面板以输入框原本的安全区域为基准，最大宽度为 560，避免把输入框横向撑开。

## v1.3.38

- 三个显示位置统一应用“用量排版”和胶囊风格：一行显示使用同款胶囊，两行显示统一的信息轨；对话框内、对话框下面和顶部任务栏不再各走不同样式。
- 两行模式将重置状态并入第二行，直接显示“今日已重置”等状态文本；“今日已重置”改用当前主题配色。
- 移除所有“检测中／处理中／完成／中断”任务状态显示。

## v1.3.37

- 聊天对话框内的两行信息改为以左侧“＋”工具栏横线所在的中轴对称排布；上下两行统一字号并略微放大，视觉中心不再偏高。

## v1.3.36

- “聊天对话框内”新增“一行概览／两行详情”切换，默认两行；两行以输入框工具栏中轴排布，第一行显示套餐、5H 和周额度，第二行显示重置券、累计 Token 和 Tibo 重置雷达。
- 修复内联设置页“彩字”按钮点击无响应的问题。

## v1.3.35

- 重新设计“聊天对话框内”的信息层：两行额度改为紧凑的信息轨，重要额度更醒目、次要字段更轻；圆角、小圆角矩形与无胶囊三种风格在此位置同样生效。安全区域同步放宽至“完全访问”后、模型控件前的完整空白区。
- 设置齿轮改为独立的主题色尾部按钮，和信息轨保持一致的边框与留白，点击区域不再被额度文字挤占。

## v1.3.34

- “聊天对话框内”恢复右侧小型设置齿轮：始终位于输入框安全区域的最右边，并为其预留宽度，额度文字不会覆盖。

## v1.3.33

- 修复“聊天对话框内”在白色输入框上文字过浅的问题：各主题会保留自身色相，并自动使用足够深的前景色，橙色、粉色和自定义主题都清晰可见。

## v1.3.32

- “聊天对话框内”改为固定在“完全访问”和右侧模型控件之间：隐藏计划名称，额度字段按两行展示，避免覆盖 Codex 自身按钮。
- 修复底部模式打开或收起设置页时的闪动：设置面板会先保持底栏锚点，再向上展开或收回。

## v1.3.31

- 设置齿轮改为主题色渐变：荧光蓝、磨砂、渐变橙、渐变粉、轻盈白、自定义与彩字都会随当前主题切换。

## v1.3.30

- 雷达弹窗的中英文混排改为按语言字形分别做视觉居中；英文标题、数字与中文内容现在处于同一视觉中线。

## v1.3.29

- 雷达弹窗两行文字改为按真实字形上下居中，解决不同 DPI 或字体下视觉偏高、偏低的问题。
- 内联设置面板随 Codex 可用宽度自适应：窄窗口会收紧字段、刷新控件和底部操作按钮，宽窗口保持清晰的最大阅读宽度。

## v1.3.28

- 重置雷达弹窗按显示位置展开：顶部任务栏模式向上展示；聊天对话框内、聊天对话框下面模式均向下展示，不再压住输入框内容。

## v1.3.27

- 修复“聊天对话框下面”模式：为输入框工具栏预留固定空间；即使系统只能识别内部编辑区域，用量栏也会紧贴完整输入框的外沿下方显示。

## v1.3.26

- 显示位置拆分为“顶部任务栏”“聊天对话框内”“聊天对话框下面”；对话框内模式与“完全访问”同一行，下面模式贴住整个输入框外沿。

## v1.3.25

- 底部用量、重置信号和齿轮改为一个整体，始终相对对话输入框水平居中并统一底边；Token 后紧贴重置信号。

## v1.3.24

- 底部栏改为以 Codex 对话输入框为锚点，窗口缩放后会重新居中并贴在输入框下方。
- 新增“轻盈白”主题：白色卡片、细灰描边、紫蓝绿橙状态点与紫色渐变齿轮。

## v1.3.23

- 底部栏进一步缩小并贴齐输入框下沿：用量胶囊与“今日已重置”紧贴排列，刷新图标收紧，设置齿轮改为紫色渐变。

## v1.3.22

- 底部“今日已重置”取消绿色语义色，卡片、文字与刷新图标均与前方用量使用相同主题配色。

## v1.3.21

- “今日已重置”和设置齿轮同步底部胶囊风格：圆角、小圆角矩形或无胶囊纯文字；整行元素改为底边对齐，取消底部视觉留白。

## v1.3.20

- 底部模式增加胶囊风格切换：圆角、小圆角矩形（默认）和无胶囊纯文字；选择会保存在设置中。

## v1.3.19

- 底部胶囊取消底部留白，整行与实际对话界面窗口底边对齐；移除任务状态，不再显示“处理中”或“完成”。

## v1.3.18

- 底部胶囊从输入区上方下移，固定贴近实际对话界面的窗口底部；展开设置仍向上展开。

## v1.3.17

- 移除“两行”用量格式，所有位置统一保持单行；旧版两行配置不会再生效。
- 底部胶囊改为小圆角矩形并缩短间距；累计 Token 胶囊仅显示数值，例如 `136.5亿`。

## v1.3.16

- “底部胶囊”仅在 Codex 的实际对话输入区出现时展示；设置、插件、项目等非对话页面会自动隐藏，避免遮挡页面设置。
- 底部胶囊模式展开设置时，设置面板向上展开，底部保留胶囊行；胶囊不再带左侧圆点，任务完成时不再显示“完成”，雷达状态和设置齿轮靠右对齐。

## v1.3.15

- 新增“底部胶囊”显示位置：在 Codex 窗口输入区上方浮动展示 `PRO`、`5H`、`周`、`重置券`、`Token`（累计 Token），保留雷达、任务状态和设置入口。
- 用量字段统一使用中文全角冒号；两行格式中 `PRO |` 跨两行垂直居中，`5H：` 与 `周：` 保持同列对齐。

### 致谢

谢谢你提醒我。

## v1.3.14

- 两行用量格式保持字段顺序：`PRO`、`5H`、`周`、`重置券`、`累计Token`；`5H：` 与 `周：` 使用同一标签列对齐。
- `今日已重置` 状态不再显示左侧绿色圆点，主条、顶部雷达悬浮窗和设置内雷达卡片均不会留下空位。
- 保留 Plus 的 5H 额度显示与 Pro 的 `5H：无限制` 显示规则。

### 致谢

感谢大佬 **猫de天空之城**、**乐缘** 提供 Bug 解决方案及验证。
